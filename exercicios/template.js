/* 
const rl = require('readline').createInterface({
    input: process.stdin,
    output: process.stdout
  })
  
  
  function solve(n) {
    pass
  }
  
  rl.once('line', (_) => { 
    
    rl.on('line', (txt) => {
      // console.log(txt);
  
      console.log(solve(parseInt(txt)));
    });
  });
  
  
  console.log("ultimo dos ultimos")

*/

var input = require('fs').readFileSync('stdin', 'utf8');

a = input

console.log(a)